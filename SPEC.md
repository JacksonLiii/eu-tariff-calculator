# 项目简报：跨境独立站关税落地成本计算器（Chrome插件 MVP）

## 一句话定位
帮欧美独立站/DTC卖家在浏览产品页/后台时，快速算出"这单卖到欧盟，加上新关税规则后，落地成本到底是多少"，不用离开浏览器去查政策文档或用Excel手算。

## 为什么是现在（不要删这段，做免责声明和文案时要用）
- 2026年7月1日起，欧盟取消€150以下包裹的关税豁免，改为对每个HS6品类征收€3固定关税（按品类线收费，不是按包裹收——一个包裹3个不同HS6品类=3次€3）。规则来自Council Regulation (EU) 2026/382，2026年2月11日正式批准。
- 一笔约€2/包裹的清关处理费预计2026年11月上线，**目前尚未正式生效**，插件里要明确标注"预计"而非"已生效"。
- VAT依然从第一欧元起征，不受本次改革影响，各成员国税率不同（如德国19%、法国20%、意大利22%）。
- 这个规则太新，现有关税工具大多没更新，是一个"规则刚变、工具真空期"窗口，越早上线越占位。

## 目标用户
卖低客单价商品到欧盟消费者的独立站/DTC卖家（Shopify、WooCommerce为主），客单价通常在€10-150之间，对这次改革最敏感。

## MVP 功能范围（严格收窄，不要一开始就扩）

**必须做：**
1. 弹出式计算器（popup.html），输入：
   - 商品申报价值（数字，支持EUR/USD切换，用固定汇率或不做自动汇率转换，MVP先只支持EUR）
   - 目的地国家（下拉选择EU成员国，内置各国VAT税率表）
   - 包裹内不同HS6品类数量（默认1，旁边加一句解释"不同商品但同一分类算1个"）
2. 输出：
   - 关税（€3 × 品类数量）
   - VAT（申报价值 × 目的地国VAT税率）
   - 落地总成本 + 占申报价值的百分比
   - 一行小字："清关处理费(~€2/包裹)预计2026年11月上线，暂未计入"
3. 本地历史记录（chrome.storage.local存最近20次计算，不需要账号系统）
4. 每条结果旁边放"数据来源"链接和"最后核实日期"，明确写"仅供参考，非正式关务/税务意见"

**这一版不做（写清楚防止范围蔓延）：**
- 批量CSV导入
- 和Shopify/WooCommerce后台自动同步订单
- 多国税率自动更新抓取（先手动维护一张静态表）
- 团队/多账号功能
- 美国关税计算（先专注欧盟这一次新规则，验证完再扩）

## 技术栈要求
- Manifest V3
- 纯前端，不需要后端服务器
- 原生HTML/CSS/JS或轻量框架均可，避免过度工程化
- 数据（VAT税率表、关税规则参数）用一个本地JSON文件维护，方便后续手动更新
- 存储用 chrome.storage.local，不需要用户登录

## 变现方案（不依赖后端）
- 免费版：每天5次计算
- Pro版：一次性付费解锁无限次数 + 历史记录导出CSV
- 建议用 Lemon Squeezy 或 Gumroad 的 License Key API 做客户端校验

## 建议的迭代顺序
1. 先在普通网页里把计算逻辑写对、测试清楚（不含插件外壳）
2. 包装成 Manifest V3 Chrome插件，popup UI 跑通
3. 加本地历史记录
4. 加免费次数限制 + 付费解锁
5. 做图标、Chrome Web Store 上架文案、隐私政策

## 风险提示
- 这是一个"算错了=用户真金白银损失"的工具，务必保留"仅供参考"免责声明，不要说"保证准确"或"官方认证"
- €2清关费11月才可能生效，上线时如果还没生效，记得回来更新
- VAT计税基础等细节建议上线前找懂欧盟VAT的人核实一遍